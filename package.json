{
  "name": "carbon-plus-root",
  "version": "1.0.0",
  "description": "C.A.R.B.O.N+ Carbon Awareness, Reduction & Behavioral Optimization Navigator Root",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "test": "npm test --prefix backend",
    "test:coverage": "npm test --prefix backend -- --coverage"
  }
}
